# Copy to .env.local and fill the values before running locally or in Docker
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
GOOGLE_AI_API_KEY=

# Optional: Supabase local DB (for docker-compose db)
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=campusconnect
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
